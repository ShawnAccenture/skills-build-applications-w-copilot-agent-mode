# Required for Codespaces-backed API calls.
# Set this to the name of your GitHub Codespace, for example:
# VITE_CODESPACE_NAME=my-codespace
# If it is not set, the app falls back to http://localhost:8000.
VITE_CODESPACE_NAME=your-codespace-name
